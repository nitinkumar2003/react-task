import styles from './index.module.css'

export default function ProductSearchFilterPagination() {
  return (
    <div className={styles.wrapper}>
      <p className={styles.placeholder}>
        🚧 Not implemented yet — build "Product Search + Filter + Pagination" here.
      </p>
    </div>
  )
}
