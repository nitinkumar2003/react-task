import styles from './index.module.css'

export default function CrudFavoritesStore() {
  return (
    <div className={styles.wrapper}>
      <p className={styles.placeholder}>
        🚧 Not implemented yet — build "Fetch + CRUD with Favorites" here.
      </p>
    </div>
  )
}
