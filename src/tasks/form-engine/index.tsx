import styles from './index.module.css'

export default function FormEngine() {
  return (
    <div className={styles.wrapper}>
      <p className={styles.placeholder}>
        🚧 Not implemented yet — build "JSON Schema Form Engine" here.
      </p>
    </div>
  )
}
