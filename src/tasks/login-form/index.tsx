import styles from './index.module.css'

export default function LoginForm() {
  return (
    <div className={styles.wrapper}>
      <p className={styles.placeholder}>
        🚧 Not implemented yet — build "Login Form" here.
      </p>
    </div>
  )
}
